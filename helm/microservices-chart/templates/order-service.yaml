apiVersion: v1
kind: Service

metadata:
  name: {{ .Values.order.name }}

spec:
  selector:
    app: {{ .Values.order.name }}

  ports:
    - protocol: TCP
      port: {{ .Values.order.port }}
      targetPort: {{ .Values.order.port }}